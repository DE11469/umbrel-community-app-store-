version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: fixedcoin_server_1
      APP_PORT: 8080

  server:
    build: .
    init: true
    volumes:
      - ${APP_DATA_DIR}/data:/data
