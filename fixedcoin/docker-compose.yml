version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: fixedcoin_fixedcoin_node_1
      APP_PORT: 8080

  fixedcoin_node:
    build: .
    init: true
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      RPC_USER: umbrel
      RPC_PASSWORD: changeme
