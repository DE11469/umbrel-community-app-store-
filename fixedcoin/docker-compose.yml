version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: fixedcoin_fixedcoin_node_1
      APP_PORT: 8080

  fixedcoin_node:
    image: alpine:latest
    init: true
    volumes:
      - ${APP_DATA_DIR}/data:/data
    command: ["sleep", "infinity"]
